apiVersion: v1
kind: Secret
metadata:
  name: twitch-secrets
  namespace: eightbitsaxlounge
  labels:
    app: ui
type: Opaque
data:
  # Base64 encoded values - replace with your actual encoded secrets
  token: "YOUR_TWITCH_TOKEN_BASE64_ENCODED"
  client_id: "YOUR_TWITCH_CLIENT_ID_BASE64_ENCODED"