[defaults]
inventory = /etc/ansible/hosts
remote_tmp = /tmp/.ansible
pipelining = True
host_key_checking = False
retry_files_enabled = False
forks = 10

[privilege_escalation]
# Use sudo by default for become
become_method = sudo
become_ask_pass = False
