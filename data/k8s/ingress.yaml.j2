apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: data-ingress
  labels:
    app: eightbitsaxlounge
    component: data
spec:
  ingressClassName: nginx
  rules:
  - host: {{ data_ingress_host }}
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: data-service
            port:
              number: 80
